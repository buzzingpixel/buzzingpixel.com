{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug Docker",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/opt/project": "${workspaceRoot}"
            },
            "xdebugSettings": {
                "show_hidden": 1
            },
        },
    ]
}
